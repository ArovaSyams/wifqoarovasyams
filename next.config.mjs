/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['media.licdn.com', 'github.com', 'raw.githubusercontent.com', 'github.githubassets.com'], // Add any other domains you need
    },
};

export default nextConfig;
